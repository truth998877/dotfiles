#!/bin/bash
set -e

for i in .bashrc .dircolors .inputrc .profile .bash_profile; do
  rm "$HOME/$i"
  if [[ -z "$HOME/$i-orig" ]] ; then 
    cp -p "$HOME/$i-orig" "$HOME/$i"
    chmod 644 "$HOME/$i"
  fi
done

for i in vim tmux;do
  cd "$i" && ./unsetup
  cd -
done 


