if [[ "$END" == work ]];then
#----WORK SPECIFIC ENV VARS-----
    export GITUSER="truth998877"   #add git user name
    export GHREPOS="$REPOS/github.com/$GITUSER" 
    export GLREPOS="$REPOS/gitlab.com/$GITUSER" 
    export PGM="$GHREPOS/pgm"
    

    export LOGBOOKS="$HOME/logbooks"
    export LOGBOOKSINDEX="$HOME/logbooks.json"
    export SCRIPTSINDEX="$HOME/scripts-index.json"
    export NOTEBOOKS="$HOME/notebooks"
    export NOTEBOOKSINDEX="$HOME/notebooks.json"
		
		export CDPATH="$CDPATH:$GHREPOS:$GLREPOS:$PGM:$LOGBOOKS:$NOTEBOOKS"
#-----

#-----WORK SPECIFIC FUNCTIONS-----
#-----


#-----WORK SPECIFIC ALIASES-----
#-----

#-----WORK SPECIFIC PATH-----
export PATH=$PATH
#-----

fi

